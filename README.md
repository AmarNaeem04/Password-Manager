# Password-Manager
A simple Tkinter Password Manager that generates strong random passwords, copies them to the clipboard, and saves/retrieves login details (website, email, password) locally using a JSON file with built-in input validation and error handling.
